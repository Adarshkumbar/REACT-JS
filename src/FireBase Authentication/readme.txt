put code in index.js to run it

import React from "react";
import ReactDOM from "react-dom/client";

import App from './FireBase Authentication/App'

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
